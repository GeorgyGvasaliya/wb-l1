package main

func main() {

	/*
		var justString string
		func someFunc() {
			v := createHugeString(1 << 10)
			justString = v[:100]
		}
		func main() {
			someFunc()
		}
	*/
	// тут глобальная переменная, что есть плохо, нужно её запихнуть внутрь функции
}
